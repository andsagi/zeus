import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zeuselity.app',
  appName: 'Zeus App - IA Profissional',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
